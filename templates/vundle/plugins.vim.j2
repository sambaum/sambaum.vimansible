" {{ ansible_managed }}
" Source all the plugin files again, this time loading their configuration.
for file in split(glob('~/{{ vimansible_vim_home_dir_short_path }}/vundle_plugins/*.vim'), '\n')
  exe 'source' file
endfor
